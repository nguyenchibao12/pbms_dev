// TODO (Module A2 — Nhật ký hệ thống): đồng đội thay nội dung stub này.
// API backend sẵn sàng: GET /admin/audit-logs (hỗ trợ lọc + phân trang qua query params).
// Đây là trang CHỈ XEM (không CRUD): bảng danh sách + bộ lọc + phân trang.
export default function AuditLogsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight text-slate-900">Nhật ký hệ thống</h1>
      <p className="mt-2 text-sm text-slate-500">
        Trang đang được phát triển (Module A2). Sẽ gồm: bảng lịch sử thao tác, bộ lọc và phân trang.
      </p>
    </div>
  );
}
