// TODO (Module A1 — Quản lý người dùng): đồng đội thay nội dung stub này.
// API backend sẵn sàng: GET /admin/users (danh sách), POST /admin/users (tạo),
//   PATCH /admin/users/:id (khóa/mở, đổi vai trò), GET /admin/users/roles (danh sách vai trò).
// Tham khảo khuôn CRUD ở các trang trong pages/manager (FloorsPage, VehicleTypesPage).
export default function UserManagementPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight text-slate-900">Quản lý người dùng</h1>
      <p className="mt-2 text-sm text-slate-500">
        Trang đang được phát triển (Module A1). Sẽ gồm: danh sách người dùng, tạo tài khoản,
        khóa/mở, đổi vai trò.
      </p>
    </div>
  );
}
